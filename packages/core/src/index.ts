export { sync, getStatus } from './sync';
export { detectUpstream, setupUpstream, createGit } from './upstream';
export { DEFAULT_OPTIONS } from './types';
export type { SyncOptions, SyncResult, ForkStatus } from './types';
